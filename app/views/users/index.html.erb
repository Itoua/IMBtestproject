<p id="notice"><%= notice %></p>
<div>
  <% if policy(current_user).new? %>
    <span class="btn btn-default navbar-btn"><%= link_to 'New User', new_user_path %></span>(Only an admin can create a new user after loging in, Non admin can't see this button)
  <% end %>
</div>

<div class="table-responsive">
  <table class="table table-condensed">
    <thead>
      <tr>
        <th>Name</th>
        <th>Surname</th>
        <th>Email</th>
        <th>Mobile number</th>
        <th>Admin</th>
        <th></th>
      </tr>
    </thead>

    <tbody>
      <% @users.each do |user| %>
        <tr>
          <td><%= user.name %></td>
          <td><%= user.surname %></td>
          <td><%= user.email %></td>
          <td><%= user.mobile_number %></td>
          <td><%= user.admin %></td>
          <% if policy(user).show? %>
            <td class="btn btn-default navbar-btn"><%= link_to 'Show', user %></td>
          <% end %>
          <% if policy(user).edit? %>
            <td class="btn btn-default navbar-btn"><%= link_to 'Edit', edit_user_path(user) %></td>
          <% end %>
          <% if policy(user).destroy? %>
            <td class="btn btn-default navbar-btn"><%= link_to 'Destroy', user, method: :delete, data: { confirm: 'Are you sure?' } %></td>
          <% end %>
        </tr>
      <% end %>
    </tbody>
  </table>
</div>

<br>
